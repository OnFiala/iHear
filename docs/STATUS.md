# Implementation status

Updated 2026-09-11. **Implementation in progress. The local acceptance gate has not yet passed. No public application deployment.**

## Source and authority
- Host verified: MacBook-Pro, user/home ondrej /Users/ondrej. Target /Users/ondrej/iHear was initially absent.
- Authenticated GitHub login verified as OnFiala. Public repository: https://github.com/OnFiala/iHear.
- `main` holds initial ownership/documentation baseline `1279a48`. `implementation/local-milestone` is the coherent implementation branch based on main.
- Public GitHub publication is authorized. Vercel/Supabase cloud/Render deployment and paid provisioning are not.

## Implemented and verified so far
- English landing, clinician forms/cards/QR/results/report UI, patient pairing/scanner/home/event screens, responsive soft glass CSS and locally bundled fonts.
- Original image generated by the available Image Generation tool; prompt/provenance in DESIGN_SYSTEM.md. Code-rendered app icons.
- getUserMedia + AudioWorklet PCM ring buffer, actual pre/post lengths, explicit activation/suspension, IndexedDB pending uploads and recoverable negative-event questions.
- TypeScript check passed. Four deterministic PCM/WAV tests passed: signed sample encoding, full 5+5 seconds, short-prebuffer extension, zero-prebuffer and overlapping-press behavior.
- Browser verification against localhost returned the real landing page, expected interactive elements and no framework error overlay; desktop screenshot inspected. Full journey remains pending.
- Docker Desktop started. Local Supabase stack successfully downloaded and started. A stalled Docker credential helper was bypassed using an isolated empty `.local/docker/config.json` for public pulls; global Docker settings were not changed.
- Private HTTPS development serving uses Tailscale on dedicated port 8446. Existing port 8445 was preserved. Actual origin is in ignored `.env.local`; no public Funnel.
- OPENAI_API_KEY absent. No live Astra calls made. Official docs verified gpt-6-astra, low reasoning and Standard prices; worker keeps interpretation unavailable without credentials.

## Active integration work
- Backend owner: private schema, composite tenant foreign keys, capability authorization, pairing/revocation, real FTS, private storage, event upload idempotency, pgmq jobs and global atomic budgets. Local migration/testing in progress.
- Worker owner: real NumPy/SciPy DSP, Silero/YAMNet pinned artifacts, independent interpretation stages, PDF charts, durable leases/retries/cleanup/scheduling. Container dependencies building.
- Main: end-to-end browser tests, offline retry, malicious visitor isolation, native phone session, PDF inspection, independent review and Git integration.

## Remaining gates
A-N acceptance matrix is in docs/ACCEPTANCE.md once integrated. Do not claim Safari/iOS from desktop emulation. Owner confirmed an iPhone is available; send a concrete pairing link after backend/worker smoke test. Hearing-aid routing tests are scheduled for Monday 2026-09-14 as a manual checklist, not an automation.

Final handoff must replace this progress snapshot with exact commits, startup commands, measured checks, physical-device evidence, limits and remaining cloud allocation.
